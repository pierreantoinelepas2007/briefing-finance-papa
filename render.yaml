services:
  - type: cron
    name: briefing-finance
    runtime: python
    schedule: "0 7 * * *"  # tous les jours à 7h UTC (= 9h heure belge)
    buildCommand: pip install -r requirements.txt
    startCommand: python newsletter.py
